#include "main.h"
#include <stdio.h>
#include <stdlib.h>

/*! \ref main.h
 */
int main(const int ARGC, const char const* const* ARGV)
{
	(void) ARGC;
	(void) ARGV;

	printf("%s\n", "hello, world!");
	
	return (EXIT_SUCCESS);
}
